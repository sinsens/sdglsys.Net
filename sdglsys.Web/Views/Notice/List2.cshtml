<!DOCTYPE html>
<html class="iframe-h">

<head>
    <meta charset="UTF-8">
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no" />
    <title>查看公告</title>
    @Styles.Render("~/Content/css")
</head>

<body>
    <div class="wrap-container clearfix">
        <div class="column-content-detail">
            <form class="layui-form" action="">
                <div class="layui-form-item">
                    <h3>公告列表</h3>
                </div>
            </form>
            <div class="layui-form" id="table-list">
                <table id="notice" class="layui-table" lay-even lay-skin="nob"></table>
            </div>
        </div>
    </div>
    @Scripts.Render("~/Content/js")
    <script type="text/html" id="titleTpl">
        <a href="/notice/view/{{d.Notice_id}}" class="layui-table-link">{{d.Notice_title}}</a>
    </script>
    <script>
        layui.use('table', function () {
            var table = layui.table;
            //第一个实例
            table.render({
                elem: '#notice'
                , height: 315
                , url: '/notice/GetList/' //数据接口
                , method: 'post'
                , page: true //开启分页
                , cols: [[ //表头
                    { field: 'Notice_id', title: 'ID', width: 80, sort: true, fixed: 'left' }
                    , { field: 'Notice_title', title: '标题' }
                    , { field: 'Notice_login_name', title: '发布者', width: 120, sort: true }
                    , { field: 'Notice_post_date', title: '发布时间', sort: true }
                ]]
            });

        });
    </script>
</body>

</html>